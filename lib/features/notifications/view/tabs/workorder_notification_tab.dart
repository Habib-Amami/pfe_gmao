import 'package:flutter/material.dart';

class WorkorderNotificationsTab extends StatelessWidget {
  const WorkorderNotificationsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("expa works"),
    );
  }
}
